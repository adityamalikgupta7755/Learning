# Using Slicing


s = "aditya malik gupta"
reversed_s = s[::-1]
print(reversed_s)

# 🔎 How it works:
# s[start:stop:step]
# start → default
# stop → default
# step = -1 → go backwards
# So [::-1] means:
# Start from end → move backward → return full string



